version: v1

components:
- name: v2ray
  docker:
    image: v2fly/v2fly-core
    args:
    - run
  ports:
  - 8080
  ingresses:
  - host: v2ray
  files:
  - path: /etc/v2ray/config.json
    config-ref: v2ray.config
