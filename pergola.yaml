version: v1

components:
- name: ss
  docker:
    image: ghcr.io/shadowsocks/ssserver-rust:v1.21.2
  ports:
  - 8388
  ingresses:
  - host: ss
  files:
  - path: /etc/shadowsocks-rust/config.json
    config-ref: shadowsocks-config
