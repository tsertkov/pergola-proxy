version: v1

components:
- name: ssserver
  docker:
    file: Dockerfile.ssserver
  ports:
  - 8388
  ingresses:
  - host: ssserver
  files:
  - path: /etc/shadowsocks-rust/config.json
    config-ref: shadowsocks.config
